// * JS por defecto utiliza cjs
// * MJS es el formato de módulos de JavaScript que se utiliza en los navegadores y en Node.js a partir de la versión 12. Para usar MJS, debes asegurarte de que tu archivo tenga la extensión .mjs y que tu entorno de ejecución lo soporte.
// para utilizar ES Modules

// * CJS para utilizar CommonJS

import { sum } from './sum.mjs'

console.log(sum(1,2))